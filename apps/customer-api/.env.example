# JWT Configuration
JWT_SECRET=change-this-to-a-secure-random-string-in-production

# Server Configuration
PORT=3000

# Database Seed Configuration (REQUIRED for initial setup)
# These environment variables MUST be set before running the seed command
# The seed command will fail if these are not provided
SEED_USERNAME=demo
SEED_PASSWORD=demo123
